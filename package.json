{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "Project developed following a serie of Youtube videos by Rodrigo Manguinhos (@rmanguinho)",
  "main": "index.js",
  "scripts": {
    "test": "jest --watch --silent --colors --noStackTrace",
    "test:staged": "jest --passWithNoTests --findRelatedTests --silent --noStackTrace",
    "test:ci": "jest --coverage --silent --noStackTrace"
  },
  "standard": {
    "env": ["jest"]
  },
  "author": "sidnei-junior",
  "license": "ISC",
  "devDependencies": {
    "husky": "^4.0.0-beta.5",
    "jest": "^27.5.1",
    "lint-staged": "^9.4.3",
    "standard": "^14.3.1"
  }
}
